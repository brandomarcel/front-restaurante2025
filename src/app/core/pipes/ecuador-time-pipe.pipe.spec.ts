import { EcuadorTimePipePipe } from './ecuador-time-pipe.pipe';

describe('EcuadorTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EcuadorTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
